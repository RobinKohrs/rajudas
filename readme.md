# Still to include

- When calling `zamg_getPointData` no ndeed to include the start and end and set a possible `max-range` value to include the data from start to end!
- `makePath` should check if the path provided is a directory also! And when the directory does not exist, create it. Also recursive!
